#include "main.h"

int find_strlen(char *s);
int check_palindrome(char *s, int (len, int index));
int ls_palindrome(char *s);

/**
 * find_strlen - Length of a string
 * @s: string length
 * Return: strlen
 */
int find_strlen(char *s)
{
	int len = 0;

	if (*(s + len))
	{
		return (strlen);
	}
}
